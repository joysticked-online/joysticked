import Loading from '../loading';

export const metadata = {
  title: 'Loading Preview | Joysticked',
  description: 'Preview the retro Pac-Man loading animation'
};

export default function LoadingPreviewPage() {
  return <Loading />;
}
